import { CalculatorButton } from '../CalculatorButton';
import { Colours } from '../../../styles/variables';

export const CalculatorButtonDigit = CalculatorButton.extend`
  color: ${Colours.CuriousBlue};
  border-color: ${Colours.CuriousBlue};
`;